# Webhook Relay Operator Modernization Plan

This sequence keeps infrastructure, live-service validation, dependencies, and
controller changes independently reviewable. Update this file as each pull
request lands and record any scope changes discovered by the preceding gate.

| PR | Scope | Exit gate | Status |
| --- | --- | --- | --- |
| 1 | Establish GitHub Actions checks and an isolated k3s end-to-end harness. Build and install the current operator and chart, exercise a reconciliation without valid external credentials, retain failure diagnostics, and expose the same test through `make e2e`. | Unit tests, chart lint/render, and k3s reconciliation pass on a pull request and on `master`. | Merged ([#26](https://github.com/webhookrelay/webhookrelay-operator/pull/26)) |
| 2 | Add an opt-in production Webhook Relay validation profile with a dedicated token, unique resource names, ownership descriptions, and guaranteed cleanup. Keep secrets out of pull-request jobs and diagnostics. | Run the profile manually against `my.webhookrelay.com`; prove bucket/input/output creation, delivery-agent configuration, idempotent reconcile, and cleanup. | Merged ([#27](https://github.com/webhookrelay/webhookrelay-operator/pull/27)); [production run](https://github.com/webhookrelay/webhookrelay-operator/actions/runs/34245206696) passed and cleanup was independently verified |
| 3 | Refresh the Go toolchain and dependencies in bounded groups. Start with the Webhook Relay client and test libraries, then Kubernetes/controller-runtime. Remove obsolete replacements and legacy operator-sdk runtime coupling, and regenerate modules after each group. | Unit and k3s suites pass after every dependency group; production profile passes at the end. | Client/tooling group merged ([#28](https://github.com/webhookrelay/webhookrelay-operator/pull/28)); its [production run](https://github.com/webhookrelay/webhookrelay-operator/actions/runs/34248630993) passed. Kubernetes/controller-runtime group validated in [#36](https://github.com/webhookrelay/webhookrelay-operator/pull/36); its [production run](https://github.com/webhookrelay/webhookrelay-operator/actions/runs/34288114875) passed. |
| 4 | Add repository instructions and focused skills for operator development and debugging, shared with Claude through `CLAUDE.md`. | Skill validation passes and the guidance accurately distinguishes reconciliation from delivery. | Merged ([#29](https://github.com/webhookrelay/webhookrelay-operator/pull/29)) |
| 5 | Extend the protected production profile into a true live-delivery suite. Deploy a recording HTTP receiver in K3s, route a nonce-bearing request from the public input through the production Webhook Relay service to that receiver, and cover routing fields such as path/header/response controls and input/output `functionId`. | Assert method, path, headers, body, and nonce at the receiver; assert configured response behavior at the caller; run function cases with a dedicated fixture function; prove updates, retries, isolation, and exact cleanup. | Validated in [#31](https://github.com/webhookrelay/webhookrelay-operator/pull/31); [production run](https://github.com/webhookrelay/webhookrelay-operator/actions/runs/34274662193) passed all three delivery cases and cleanup was independently verified |
| 6 | Extend the Relay Go client with replay-on-connect output configuration and pin the operator to the merged client revision. | Client and operator unit suites pass; the dependency remains the only behavior change in the operator PR. | Client [#12](https://github.com/webhookrelay/webhookrelay-go/pull/12) and operator [#32](https://github.com/webhookrelay/webhookrelay-operator/pull/32) merged |
| 7 | Migrate output `function_id` to the consistent `functionId` CRD property with a deprecated compatibility alias, and expose current input and output controls: input TLS/path controls, response functions, retries, TLS verification, rules, durable delivery, throttling, and replay-on-connect. | Generated deepcopy and every shipped CRD agree; unit tests prove mapping, equality, defaults, and legacy alias behavior; k3s and protected production delivery pass. | Validated in [#33](https://github.com/webhookrelay/webhookrelay-operator/pull/33); [production run](https://github.com/webhookrelay/webhookrelay-operator/actions/runs/34281155185) passed live delivery, function updates, stable identities, delivery-control persistence, and exact cleanup |
| 8 | Restrict credential references to the CR namespace, add first-class agent transport settings, and verify image/resource overrides. | Cross-namespace reads fail before Kubernetes API access; generated Deployments cover WebSocket transport, image, resources, and environment overrides; k3s passes. | Validated in [#34](https://github.com/webhookrelay/webhookrelay-operator/pull/34); [production run](https://github.com/webhookrelay/webhookrelay-operator/actions/runs/34282472351) passed and cleaned up. Fixes [#19](https://github.com/webhookrelay/webhookrelay-operator/issues/19) and [#25](https://github.com/webhookrelay/webhookrelay-operator/issues/25), and regression-tests the image override needed by [#22](https://github.com/webhookrelay/webhookrelay-operator/issues/22) |
| 9 | Add bucket authentication and current bucket stream/storage controls, then refresh the root and chart READMEs with secure installation, complete CR examples, field reference, subscription-dependent behavior, migration guidance, development workflow, and troubleshooting. Audit and close every resolved or answered open issue. | Authentication values come from same-namespace Secrets; documentation examples render against the CRD; every previously open issue is linked to a fix or a concrete answer. | Validated in [#35](https://github.com/webhookrelay/webhookrelay-operator/pull/35); [production run](https://github.com/webhookrelay/webhookrelay-operator/actions/runs/34285176378) proved unauthenticated rejection, authenticated live delivery, bucket controls, stable updates, and exact cleanup. Addresses [#2](https://github.com/webhookrelay/webhookrelay-operator/issues/2), [#12](https://github.com/webhookrelay/webhookrelay-operator/issues/12), and [#24](https://github.com/webhookrelay/webhookrelay-operator/issues/24) |
| 10 | Complete the modern controller semantics after the runtime migration: add conditions/observed generation, accurate Deployment readiness, explicit API endpoint configuration for local fakes, and focused controller tests. | Unit, local fake-service integration, k3s, and live production delivery profiles pass; upgrade from chart 0.4.1 is verified. | Runtime APIs, context propagation, manager probes, Lease leader election, and controller builder are in [#36](https://github.com/webhookrelay/webhookrelay-operator/pull/36); status/readiness and upgrade coverage remain planned. |
| 11 | Modernize packaging and release controls. Harden the operator image, update Helm/CRD/RBAC assets, validate install/upgrade/uninstall, coordinate a multi-architecture relay-agent image, add multi-architecture operator builds, and document local development and rollback. | Published relay-agent and operator manifests both include amd64/arm64; release-equivalent builds and k3s install/upgrade/uninstall checks gate publication. | Relay-agent [`1.37.0`](https://github.com/rusenask/client/releases/tag/1.37.0) publishes amd64/arm64 through client [#141](https://github.com/rusenask/client/pull/141) and [#142](https://github.com/rusenask/client/pull/142), resolving [#22](https://github.com/webhookrelay/webhookrelay-operator/issues/22). Operator [`0.7.0`](https://github.com/webhookrelay/webhookrelay-operator/releases/tag/0.7.0) pins that agent version through [#38](https://github.com/webhookrelay/webhookrelay-operator/pull/38); the exact image passed [production live delivery](https://github.com/webhookrelay/webhookrelay-operator/actions/runs/34335141067). Authentication and tag-aligned recovery were fixed in [#39](https://github.com/webhookrelay/webhookrelay-operator/pull/39) and [#40](https://github.com/webhookrelay/webhookrelay-operator/pull/40), and the [published operator image](https://github.com/webhookrelay/webhookrelay-operator/actions/runs/34336637709) contains amd64/arm64. Install/upgrade/uninstall gates remain planned. |

Production validation is intentionally separated from the default pull-request
suite. It may create Webhook Relay routing resources, so it must use a dedicated
account or token and delete only resources carrying the run's unique ownership
marker.

GitHub Actions is the repository's sole CI system. Workflows from all outside
collaborators require maintainer approval before execution; the former Drone
webhook is disabled and its pipeline configuration has been removed.

## Remaining execution plan

The remaining work is intentionally split so controller behavior, cluster
lifecycle, and publication policy can be reviewed and rolled back
independently. Every implementation pull request must receive an independent
agent review, address all actionable findings, pass its pull-request gates,
and update this table after merge.

The next coordinated release reserves operator `0.8.0` and Helm chart `0.6.0`;
the already-published chart `0.5.0` must never be overwritten. The release tag
must equal `Chart.appVersion` and the operator image tag, while `Chart.version`
remains independently versioned. The relay-agent version is read from the
shipped operator default and validated rather than assumed to match either.

| Phase | Pull request scope | Exit gate | Status |
| --- | --- | --- | --- |
| 12 | Bump the candidate chart to `0.6.0`. Add an operator-admin-only Relay API endpoint override (environment/Helm configuration, never a CR field) and a stateful fake Relay API used by isolated K3s. Replace the deliberately failing isolated reconcile with deterministic create, update, idempotency, error, and cleanup assertions. | Unit tests validate the production default, absolute HTTP(S) URL and no-userinfo rules, proxy composition, and absence of credentials from logs/status. Isolated K3s proves remote-style convergence without production credentials; production delivery remains separate. | Merged ([#43](https://github.com/webhookrelay/webhookrelay-operator/pull/43)); [CI](https://github.com/webhookrelay/webhookrelay-operator/actions/runs/34352337807) passed unit/lint, Helm, and isolated K3s create/update/output-prune/input-retention/idempotency/failure/recovery coverage |
| 13 | Add Kubernetes-standard `RoutingReady`, `AgentReady`, and aggregate `Ready` conditions, top-level and per-condition `observedGeneration`, and actual Deployment rollout readiness while retaining existing status fields as compatibility mirrors. Watch owned Deployment changes, handle missing/unobserved/partial/available/`ProgressDeadlineExceeded` states, retry status conflicts, and return Deployment reconcile failures. | Focused tests cover condition reasons, stable transition times/no write loop, generation changes, status conflicts, deletion, routing failures, and legacy mirrors. Generated CRDs agree; isolated K3s proves false-to-true readiness and update transitions; protected production delivery passes. | Merged ([#44](https://github.com/webhookrelay/webhookrelay-operator/pull/44)); [CI](https://github.com/webhookrelay/webhookrelay-operator/actions/runs/34355001359) and [protected production delivery](https://github.com/webhookrelay/webhookrelay-operator/actions/runs/34355358893) passed |
| 14 | Remove/deprecate the duplicate Helm-managed CRD template and define `crds/` as the supported retained CRD path. Add a packaged-chart lifecycle suite: clean install, CR deletion and owned Deployment garbage collection, Helm uninstall with CRD retention, and separately guarded CRD removal only in disposable K3s. Upgrade from the checksum-pinned published chart `0.4.1`, explicitly apply the candidate CRD, and test application-only safe rollback after scaling the operator to zero. | CI verifies checksum `7de5d0afa11405d603c41b81bab1b33f93e0cf6f9ddb3fc7ae5e9a1fd85a4907`, Helm revision transitions, CR UID and legacy `function_id`, Lease/probes/RBAC/schema, zero old/new pod overlap, old-controller-compatible rollback fixtures, no remote duplication, namespaced cleanup, CRD retention, and guarded test teardown. Documentation states CRD upgrade, rollback limitations, and uninstall behavior. | In progress; depends on phase 13 |
| 15 | Replace legacy chart scripts with deterministic GitHub Actions packaging/publication. Pin tooling, validate the explicit operator `0.8.0` / chart `0.6.0` mapping, refuse overwriting an existing version, produce chart SHA256/checksum metadata, and expose reusable publish steps without creating a GitHub release. | Packaged chart passes lint and lifecycle tests; retries accept only an already-published artifact with the identical digest; remote index URL/digest match local output. Configure a protected `release` environment with required reviewers, scoped Docker credentials, and a scoped charts storage identity before enabling writes. | Planned; depends on phase 14; release environment and storage identity required |
| 16 | Orchestrate publication from an operator tag reachable from `master`: validate explicit operator/chart/agent mapping; build immutable image and chart artifacts; assert exact amd64/arm64 platform sets; smoke-run both platforms for the operator and both pinned agent variants; run packaged-chart K3s and protected production live delivery against immutable tags; publish the chart; promote `latest`; and create the GitHub release last with chart/checksum assets and image digests. | A dry-run fixture proves job ordering. Build, validation, or chart-publication failures cannot create a public release or move `latest`. If the final release API call fails after promotion, recovery must resume from the digest-verified immutable artifacts without rebuilding or changing `latest`. The production gate includes function behavior and exact cleanup. Release notes record operator, chart, and agent versions plus artifact digests. | Planned; depends on phase 15 |

Known follow-ups outside this sequence are per-object API clients for safe
concurrent reconciliation and Secret watches/cache invalidation for credential
rotation. They should remain separate because neither is required to make
status, upgrade, or release behavior truthful.
