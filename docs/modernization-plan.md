# Webhook Relay Operator Modernization Plan

This sequence keeps infrastructure, live-service validation, dependencies, and
controller changes independently reviewable. Update this file as each pull
request lands and record any scope changes discovered by the preceding gate.

| PR | Scope | Exit gate | Status |
| --- | --- | --- | --- |
| 1 | Establish GitHub Actions checks and an isolated k3s end-to-end harness. Build and install the current operator and chart, exercise a reconciliation without valid external credentials, retain failure diagnostics, and expose the same test through `make e2e`. | Unit tests, chart lint/render, and k3s reconciliation pass on a pull request and on `master`. | Merged ([#26](https://github.com/webhookrelay/webhookrelay-operator/pull/26)) |
| 2 | Add an opt-in production Webhook Relay validation profile with a dedicated token, unique resource names, ownership descriptions, and guaranteed cleanup. Keep secrets out of pull-request jobs and diagnostics. | Run the profile manually against `my.webhookrelay.com`; prove bucket/input/output creation, delivery-agent configuration, idempotent reconcile, and cleanup. | Merged ([#27](https://github.com/webhookrelay/webhookrelay-operator/pull/27)); [production run](https://github.com/webhookrelay/webhookrelay-operator/actions/runs/34245206696) passed and cleanup was independently verified |
| 3 | Refresh the Go toolchain and dependencies in bounded groups. Start with the Webhook Relay client and test libraries, then Kubernetes/controller-runtime. Remove obsolete replacements and legacy operator-sdk runtime coupling, and regenerate modules after each group. | Unit and k3s suites pass after every dependency group; production profile passes at the end. | Client/tooling group merged ([#28](https://github.com/webhookrelay/webhookrelay-operator/pull/28)); [production run](https://github.com/webhookrelay/webhookrelay-operator/actions/runs/34248630993) passed. Kubernetes/controller-runtime group is in review ([#36](https://github.com/webhookrelay/webhookrelay-operator/pull/36)). |
| 4 | Add repository instructions and focused skills for operator development and debugging, shared with Claude through `CLAUDE.md`. | Skill validation passes and the guidance accurately distinguishes reconciliation from delivery. | Merged ([#29](https://github.com/webhookrelay/webhookrelay-operator/pull/29)) |
| 5 | Extend the protected production profile into a true live-delivery suite. Deploy a recording HTTP receiver in K3s, route a nonce-bearing request from the public input through the production Webhook Relay service to that receiver, and cover routing fields such as path/header/response controls and input/output `functionId`. | Assert method, path, headers, body, and nonce at the receiver; assert configured response behavior at the caller; run function cases with a dedicated fixture function; prove updates, retries, isolation, and exact cleanup. | Validated in [#31](https://github.com/webhookrelay/webhookrelay-operator/pull/31); [production run](https://github.com/webhookrelay/webhookrelay-operator/actions/runs/34274662193) passed all three delivery cases and cleanup was independently verified |
| 6 | Extend the Relay Go client with replay-on-connect output configuration and pin the operator to the merged client revision. | Client and operator unit suites pass; the dependency remains the only behavior change in the operator PR. | Client [#12](https://github.com/webhookrelay/webhookrelay-go/pull/12) and operator [#32](https://github.com/webhookrelay/webhookrelay-operator/pull/32) merged |
| 7 | Migrate output `function_id` to the consistent `functionId` CRD property with a deprecated compatibility alias, and expose current input and output controls: input TLS/path controls, response functions, retries, TLS verification, rules, durable delivery, throttling, and replay-on-connect. | Generated deepcopy and every shipped CRD agree; unit tests prove mapping, equality, defaults, and legacy alias behavior; k3s and protected production delivery pass. | Validated in [#33](https://github.com/webhookrelay/webhookrelay-operator/pull/33); [production run](https://github.com/webhookrelay/webhookrelay-operator/actions/runs/34281155185) passed live delivery, function updates, stable identities, delivery-control persistence, and exact cleanup |
| 8 | Restrict credential references to the CR namespace, add first-class agent transport settings, and verify image/resource overrides. | Cross-namespace reads fail before Kubernetes API access; generated Deployments cover WebSocket transport, image, resources, and environment overrides; k3s passes. | Validated in [#34](https://github.com/webhookrelay/webhookrelay-operator/pull/34); [production run](https://github.com/webhookrelay/webhookrelay-operator/actions/runs/34282472351) passed and cleaned up. Fixes [#19](https://github.com/webhookrelay/webhookrelay-operator/issues/19) and [#25](https://github.com/webhookrelay/webhookrelay-operator/issues/25), and regression-tests the image override needed by [#22](https://github.com/webhookrelay/webhookrelay-operator/issues/22) |
| 9 | Add bucket authentication and current bucket stream/storage controls, then refresh the root and chart READMEs with secure installation, complete CR examples, field reference, subscription-dependent behavior, migration guidance, development workflow, and troubleshooting. Audit and close every resolved or answered open issue. | Authentication values come from same-namespace Secrets; documentation examples render against the CRD; every previously open issue is linked to a fix or a concrete answer. | Validated in [#35](https://github.com/webhookrelay/webhookrelay-operator/pull/35); [production run](https://github.com/webhookrelay/webhookrelay-operator/actions/runs/34285176378) proved unauthenticated rejection, authenticated live delivery, bucket controls, stable updates, and exact cleanup. Addresses [#2](https://github.com/webhookrelay/webhookrelay-operator/issues/2), [#12](https://github.com/webhookrelay/webhookrelay-operator/issues/12), and [#24](https://github.com/webhookrelay/webhookrelay-operator/issues/24) |
| 10 | Complete the modern controller semantics after the runtime migration: add conditions/observed generation, accurate Deployment readiness, explicit API endpoint configuration for local fakes, and focused controller tests. | Unit, local fake-service integration, k3s, and live production delivery profiles pass; upgrade from chart 0.4.1 is verified. | Runtime APIs, context propagation, manager probes, Lease leader election, and controller builder are in [#36](https://github.com/webhookrelay/webhookrelay-operator/pull/36); status/readiness and upgrade coverage remain planned. |
| 11 | Modernize packaging and release controls. Harden the operator image, update Helm/CRD/RBAC assets, validate install/upgrade/uninstall, coordinate a multi-architecture relay-agent image, add multi-architecture operator builds, and document local development and rollback. | Published relay-agent and operator manifests both include amd64/arm64; release-equivalent builds and k3s install/upgrade/uninstall checks gate publication. | Planned; the currently published `webhookrelayd` and `webhookrelayd-ubi8` defaults are amd64-only, so [#22](https://github.com/webhookrelay/webhookrelay-operator/issues/22) cannot be closed by an operator-only code change |

Production validation is intentionally separated from the default pull-request
suite. It may create Webhook Relay routing resources, so it must use a dedicated
account or token and delete only resources carrying the run's unique ownership
marker.

GitHub Actions is the repository's sole CI system. Workflows from all outside
collaborators require maintainer approval before execution; the former Drone
webhook is disabled and its pipeline configuration has been removed.
